/* eslint-disable linebreak-style */
/* eslint-disable quotes */
/* eslint-disable linebreak-style */
// @ts-check

import _ from "lodash";

/**
 * @param {Number} num
 */
export default (num) => {
  const result = num / 2;
  _.noop();
  return result;
};
